export default function ChapterDetail({
    params,
}: {
    params: { courseId: string, chapterId: string };
}) {
    return (
        <div>
            <h1>Chapter Detail</h1>
        </div>
    );
}
